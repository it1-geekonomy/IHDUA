"use client";

/**
 * Compat entry for `@/shared/components/footer/footer`.
 * Prefer `@/shared/components/footer`.
 *
 * Note: on Windows, `Footer.tsx` collides with `footer.tsx` — use FooterShell.
 */
export { default } from "./FooterShell";
