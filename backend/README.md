# IHDUA Backend

API server for the IHDUA platform.

## Getting started

```bash
pnpm install
pnpm dev
```

Runs at [http://localhost:4000](http://localhost:4000).

## Endpoints

- `GET /health` — health check
- `GET /api` — API root
