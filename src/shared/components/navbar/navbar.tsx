"use client";

/**
 * Compat entry for `@/shared/components/navbar/navbar`.
 * Prefer `@/shared/components/navbar`.
 *
 * Note: on Windows, `Navbar.tsx` collides with `navbar.tsx` — use NavbarShell.
 */
export { default } from "./NavbarShell";
